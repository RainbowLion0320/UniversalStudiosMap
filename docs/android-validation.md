# Android 验证记录

日期：2026-09-21；应用版本 1.0.0；包名 `com.fushao.universalwander`。

## 迭代

1. 将网页三栏界面收敛为地图优先，清单按需展开；时间参数与导出统一收进更多选项。
2. 在 390 × 844 / 844 × 390 浏览器检查中修复底栏偏移、横屏摘要挤占阅读空间、地图标记重叠；渲染改为按需触发。
3. 在 Android 16 / API 36、Pixel 7 ARM64 模拟器上检查真实 WebView、系统栏、旋转和触摸。日期时间改成紧凑入口；修正窄屏文字换行与横屏 attribution 位置。

## 已验证

- Node 路线与时间估算：11 项测试通过。
- `npm run android:release`：构建成功，release 非调试包，Android 8.0+，target API 36。
- zipalign 检查通过；APK v2 / v3 签名验证通过；SHA-256 随包输出。
- `npm audit`：全部依赖 0 个已知漏洞（公开发布准备时复核）。CLI 的 xcode 间接依赖 uuid 已通过 override 升级到修复版本 11.1.1，并检查其 CommonJS / UUID 生成兼容性。
- 飞行模式、关闭 Wi-Fi / 移动数据后，全新安装签名 APK：模型、项目、步道和五站估算正常；排队来源显示离线估算。
- 在 Android 模拟器中由竖屏旋转至横屏，行程和打卡状态保留。
- 系统返回键从行程回到地图；更多选项和系统分享菜单正常打开，分享内容包含项目、时间、完成状态及来源说明。只检查系统分享面板，未向任何人发送消息。
- 同一签名覆盖安装与进程重启后，本机行程及完成状态保留。
- 浏览器：编辑日期与出发时间、上下排序、游玩节奏设置、刷新恢复通过；360 × 740 / 844 × 390 / 1440 × 1000 三个尺寸下导航均在屏幕内。

截图与操作记录在 `output/android-test/`、`output/playwright/`，构建输出在 `output/android-tools/release.log`。

## 验证边界

使用 Android 16 ARM64 模拟器验证，没有连接真实安卓手机，也未覆盖所有厂商 WebView / GPU / 系统版本。模型为艺术化地图，入口连接与临时封路仍需现场确认。
